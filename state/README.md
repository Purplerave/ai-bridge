# State slots (EICP AI Bridge transport)

One JSON file per slot. Key `a.b.c` → `state/a_b_c.json`.

Written by agents or `eicp/helper.py` (`write_state_slot`). Last successful merge to `main` wins for that file.
